// lib/config.dart

// Change this based on where your Django server is running
// localhost:8000 → for local testing on same machine
// ngrok URL or network IP (e.g., 192.168.0.x) → for cross-device testing

const String baseUrl = 'https://5f5100fcc4eb.ngrok-free.app';